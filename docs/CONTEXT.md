# Project context (durable directives all agents must always honor)

_Seeded from the goal; the architect refines this to the essence -- keep it short._

A desktop calculator app in Python with a graphical UI, in the style of the Windows Calculator: a result display and a grid of clickable buttons (digits 0-9, decimal point, + − × ÷, parentheses, C to clear, ⌫ backspace, =). Built with Tkinter from the Python standard library (no extra dependencies), runs on Windows. Supports both mouse clicks and keyboard input, correct operator precedence, decimals and parentheses. Division by zero or malformed input shows a friendly "Error" in the display and lets the user keep typing — never a crash or a stack trace.
Architecture (important): put ALL arithmetic in a pure, GUI-free `engine` module, and a headless `controller` that turns button/key presses into display state. The Tkinter layer is a thin view that only wires widgets to the controller, so the logic is fully unit-testable without a display.
Ship a `calc` entry point that opens the window (`python -m calc` or `calc`), a pytest suite covering the engine and controller (precedence, parentheses, decimals, division by zero, clear/backspace, malformed input) — no test may require a live display — and a short README with how to launch it and what the window looks like.

**Established (do not regress):**
- `calc/engine.py` (M1-real ✅): Full shunting-yard pipeline. ASCII operators, parentheses, decimals (incl. `.5` leading-dot). Correct precedence, left-associative. Division by zero / malformed / empty / non-str → `"Error"`. Never raises. `_format_result` guards empty-string edge case (subnormal floats).
- `calc/controller.py` (M2-real ✅): Full state machine. Digit append, operator replace (last-op wins), decimal guard, no operator after `(` or at start. Result continuation: digit/`.`/`(` → fresh; operator → continues. `=` delegates to engine. Error recovery: any key (except `C`/`backspace`) clears and starts fresh. `)` after result clears and ignores. Backspace after result/error clears all. ASCII internally, Unicode for display.
- `calc/view.py` (M3 ✅): Full button grid (5×4, 20 buttons), right-aligned display (Segoe UI 24pt, white bg, sunken relief), mouse + keyboard bindings (standard + numpad), wired via `press(key)` + `refresh()`. All buttons `takefocus=False`, Segoe UI 14pt. `focus_set()` in `__init__`. Window title "Calculator", non-resizable, #f0f0f0 background. M3-real polish tests (21) pass via AST analysis.
- `calc/__main__.py` + `pyproject.toml` (M4 ✅): Package wired. `python -m calc` and `calc` both launch. `pyproject.toml` build-backend = `setuptools.build_meta` (never private/internal paths). `[project.optional-dependencies]` dev extras present. README polished: title `# calc`, Usage / Window appearance / Running the tests sections, accurate ASCII-art button grid, color descriptions.
- Tests (7 files): `test_engine.py` (111), `test_controller.py` (89), `test_view_polish.py` (21), `test_view.py`, `test_view_takefocus.py`, `test_view_wiring.py`, `test_package_wiring.py`. 365 tests green. No test imports `tkinter` directly or creates a live display.
- Full API contracts are in `docs/ARCHITECTURE.md`.
